<!doctype html>
<html lang="ko">
<head>
<meta charset="utf-8">
<title>{{ headline }} | {{ campaign.advertiser }} 케이스스터디</title>
<meta name="description" content="{{ (narrative.summary or narrative.overview)[:160] }}">
<meta property="og:title" content="{{ headline }}">
<meta property="og:description" content="{{ (narrative.summary or narrative.overview)[:200] }}">
{% if hero_image_url %}<meta property="og:image" content="{{ hero_image_url }}">{% endif %}
<style>{{ css | safe }}</style>
<script type="application/ld+json">
{{ jsonld | safe }}
</script>
</head>
<body>
<article class="note">

  <header class="hdr">
    <span class="hdr__brand">{{ company.name }}</span>
    <span class="hdr__divider">▌</span>
    <span class="hdr__type">Case Study Note</span>
    <span class="hdr__meta">
      {% if campaign.campaign_no %}No. {{ campaign.campaign_no }} · {% endif %}
      {% if campaign.period_start %}{{ campaign.period_start }}{% if campaign.period_end %} – {{ campaign.period_end }}{% endif %}{% endif %}
    </span>
  </header>

  <section class="title-block">
    <h1 class="title-block__h1">{{ headline }}</h1>
    {% if subhead %}<p class="title-block__sub">{{ subhead }}</p>{% endif %}
    {% if narrative.tldr %}
    <ul class="tldr">
      {% for item in narrative.tldr %}
      <li>{{ item }}</li>
      {% endfor %}
    </ul>
    {% endif %}
  </section>

  {% if campaign.metrics_table %}
  <section class="kpis" aria-label="핵심 지표">
    {% for m in campaign.metrics_table[:4] %}
    <div class="kpi">
      <span class="kpi__label">{{ m.indicator }}</span>
      <span class="kpi__value">{{ m.value }}</span>
      {% if m.note %}<span class="kpi__note">{{ m.note }}</span>{% endif %}
    </div>
    {% endfor %}
  </section>
  {% endif %}

  <section class="body">

    {% if narrative.summary %}
    <div class="summary">
      <h3>요약</h3>
      <p>{{ narrative.summary }}</p>
    </div>
    {% endif %}

    <h2><span class="sec-num">01</span>캠페인 개요</h2>
    <p>{{ narrative.overview }}</p>

    <h2><span class="sec-num">02</span>광고 집행 배경</h2>
    <p>{{ narrative.background }}</p>

    <h2><span class="sec-num">03</span>적용 전략 · 핵심 성과</h2>
    <p>{{ narrative.strategy }}</p>

    {% set all_charts = (charts.performance or []) + ([charts.inline_strategy] if charts.inline_strategy else []) %}
    {% if all_charts %}
    <div class="figs">
      {% for ch in all_charts %}
      <figure class="fig">
        <div class="fig__head">
          <span class="fig__num">[그림 {{ loop.index }}]</span>
          <span class="fig__title">{{ ch.title }}</span>
        </div>
        <img src="data:image/png;base64,{{ ch.image_b64 }}" alt="{{ ch.title }}">
        {% if ch.caption %}<figcaption>{{ ch.caption }}</figcaption>{% endif %}
      </figure>
      {% endfor %}
    </div>
    {% elif campaign.metrics_table %}
    <table class="metrics">
      <thead>
        <tr><th>성과 지표</th><th>성과</th><th>비고</th></tr>
      </thead>
      <tbody>
        {% for m in campaign.metrics_table %}
        <tr>
          <td class="metrics__indicator">{{ m.indicator }}</td>
          <td class="metrics__value">{{ m.value }}</td>
          <td class="metrics__note">{{ m.note }}</td>
        </tr>
        {% endfor %}
      </tbody>
    </table>
    {% endif %}

    {% if narrative.insights %}
    <div class="insights-box">
      <h3>핵심 인사이트</h3>
      <ul>
        {% for item in narrative.insights %}
        <li>{{ item }}</li>
        {% endfor %}
      </ul>
    </div>
    {% endif %}
  </section>

  <footer class="ftr">
    <div class="ftr__about">
      <strong>About {{ company.name }}</strong>
      <span>{{ company.description }}</span>
    </div>
    <div class="ftr__meta">
      <span>
        <a href="{{ company.url }}">{{ company.url }}</a>
        {% if company.url_secondary %} · <a href="{{ company.url_secondary }}">{{ company.url_secondary }}</a>{% endif %}
      </span>
      <span><a href="mailto:{{ company.press_contact_email }}">{{ company.press_contact_email }}</a></span>
      <span>© {{ year }} {{ company.name }} Inc.</span>
    </div>
  </footer>
</article>
</body>
</html>
